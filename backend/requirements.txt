# ============================================================================
# AUTONOMOUS BUSINESS PLATFORM — FastAPI Backend Dependencies
# ============================================================================
# Only what api_server.py actually uses. Clean, fast deploys on Render/Railway.
# ============================================================================

# === CORE WEB FRAMEWORK ===
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# === AI — GROQ (LLaMA 3) ===
groq>=0.9.0

# === AI — REPLICATE (Images/Video) ===
replicate>=0.25.0

# === HTTP CLIENTS ===
httpx>=0.27.0
requests>=2.31.0

# === DATA & VALIDATION ===
pydantic>=2.5.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# === UTILITIES ===
pillow>=10.0.0
aiofiles>=23.2.1
python-multipart>=0.0.9
